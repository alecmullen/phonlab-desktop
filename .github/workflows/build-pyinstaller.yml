on:
  push:
    branches: [ "main" ]

jobs:
  pyinstaller-build-mac:
    runs-on: macos-latest
    steps:
      - name: Create Executable
        uses: sayyid5416/pyinstaller@v1
        with:
          python_ver: '3.12.10'
          spec: 'src/phonlab_desktop.py'
          requirements: 'requirements.txt'
          upload_exe_with_name: 'Phonlab'
          options: --name "Phonlab Mac", --windowed, --one-file

  pyinstaller-build-windows:
    runs-on: windows-latest
    steps:
      - name: Create Executable
        uses: sayyid5416/pyinstaller@v1
        with:
          python_ver: '3.12.10'
          spec: 'src/phonlab_desktop.py'
          requirements: 'requirements.txt'
          upload_exe_with_name: 'Phonlab'
          options: --name "Phonlab Windows", --windowed, --one-file

  pyinstaller-build-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Create Executable
        uses: sayyid5416/pyinstaller@v1
        with:
          python_ver: '3.12.10'
          spec: 'src/phonlab_desktop.py'
          requirements: 'requirements.txt'
          upload_exe_with_name: 'Phonlab'
          options: --name "Phonlab Linux", --windowed, --one-file
          
